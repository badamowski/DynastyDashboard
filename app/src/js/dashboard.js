app.controller('DashboardController', function($scope, $routeParams, $location, $rootScope, $timeout) {

	$scope.init = function(){
		if($rootScope.user && $rootScope.user.mfl){

		}
	};

	$scope.mflLogin = function(){
		
	};
});